services:
  stock-chess:
    container_name: stock-chess
    image: stock-chess
    build:
      context: ../..
      dockerfile: assets/docker/Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
